# Rendered evidence — recall history on send attempt

This branch exists only to host rendered evidence images referenced from a pull
request description. It carries no source code and is not intended to be merged.

| File | What it shows |
| --- | --- |
| `before.gif` | Pre-fix: a lost prompt is unrecoverable; the first Up-Arrow yields an older prompt. |
| `after.gif` | Post-fix: the first Up-Arrow recovers the lost prompt. |
| `verify-before-strip.png` | Annotated frame strip of the pre-fix run, with per-frame state. |
| `verify-after-strip.png` | Annotated frame strip of the post-fix run, with per-frame state. |

Both recordings were produced by the committed capture entry on the fix branch,
so they are re-recordable from source rather than hand-made.
